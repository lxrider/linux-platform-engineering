# === Secrets & identifiants — JAMAIS committés =========================
*.creds
*.key
*.pem
id_*
!*.pub
secrets/

# === Environnements réels (on ne versionne que les *.example) ==========
*.env
!*.env.example
env/*.env
!env/*.env.example

# === Inventaires réels =================================================
inventory/hosts
inventory/*.real

# === Artefacts lourds / générés =======================================
*.qcow2
*.iso
*.img
*.log
*.tmp
